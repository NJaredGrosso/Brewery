/*
while (true) {
    let funcion = prompt("Ingrese la funcion que quiere correr")
    
    switch (funcion) {
        case "if":
            //IF

            let a = prompt("ingrese un número distinto de 0");

            if(a != 0){
                alert("Número ingresado: " + a);
            }else{
                alert("Valor no valido");
            }
            break;

        case "for":
            //FOR

            for (let b = 0; b < 11; b++) {
                
                alert("La cuenta va en: " + b)

            }

            break;

        case "while":
            //WHILE

            let usuario = prompt("Ingrese usuario");
            let baneado = "juan";

            while(usuario != baneado){
                alert("Bienvenido " + usuario);
                usuario = prompt("Ingrese otro usuario")
                if(usuario = ESC){ //agregamos un break
                    break;
                }
            }

            alert("Usuario no permitido");

            break;

        default:
            alert("funcion no aprendida");
            break;
    }

}
*/




